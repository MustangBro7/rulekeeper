# Agent playbook

Every implementation must preserve the privacy boundary and keep all raw source material on the local machine.

Use npm as the package manager.

Run `npm test` after editing source code.
