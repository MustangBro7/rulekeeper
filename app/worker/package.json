{
  "name": "rulekeeper-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit",
    "test": "wrangler types && node --experimental-strip-types --test test/*.test.mjs",
    "cf-typegen": "wrangler types"
  }
}
